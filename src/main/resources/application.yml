spring:
  application:
    name: Q&A
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/db_wx_project?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

---
server:
  port: 10000
